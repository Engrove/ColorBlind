{
  "name": "color-name-camera",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Mobile camera app that detects exact color name and color family from RGB samples.",
  "scripts": {
    "data:update": "node scripts/update-colors.mjs",
    "build": "node scripts/update-colors.mjs && node scripts/build.mjs",
    "build:offline-sample": "node scripts/build.mjs",
    "dev": "node scripts/dev-server.mjs",
    "check": "node --check public/app.js && node --check scripts/update-colors.mjs && node --check scripts/build.mjs && node --check scripts/dev-server.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
